# Common app

Reusable technical building blocks for the backend.

## Current contents
- Abstract base models in `models.py`.
- Shared Celery placeholder task in `tasks.py`.

Keep this app lightweight and focused on cross-cutting backend concerns.
