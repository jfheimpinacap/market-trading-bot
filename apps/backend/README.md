# Backend app

Django + Django REST Framework API scaffold for the market-trading-bot monorepo.

## Scope today
- Modular Django project with split settings.
- Healthcheck endpoint at `/api/health/`.
- PostgreSQL, Redis, and Celery configuration via environment variables.
- No authentication, provider integrations, or trading logic yet.
